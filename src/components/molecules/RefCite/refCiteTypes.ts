/** Shared citation payload for HTML RefCite and SVG SvgRefCite. */
export type RefCiteItem = {
  n: number;
  author: string;
  date: string;
  dateLabel: string;
  publisher: string;
  title: string;
  summary: string;
};
